# 🚀 迭代更新笔记 (Update Notes)
- **✨ 核心功能升级**：新增近 7 日历史趋势展示、sparkline 走势摘要、学生资金分层、风险画像、热门理财选项地图、理财思维训练，以及零依赖静态前端工作台。
- **🛠️ 架构与代码重构**：把项目整理成明确的包结构，拆出 `history.py`、`insights.py`、`catalog.py`，主流程只负责编排；同时保留旧入口兼容层，避免迁移断档。
- **🐛 细节与健壮性优化**：补了 BaoStock / akshare 获取失败时的兜底处理，历史文件损坏时自动备份，测试覆盖估值边界、历史趋势、风险画像和资金分层。
- **💡 架构师建议**：下一步可以接入 GitHub Pages，把 `web/` 作为公开演示页；也可以增加 `--export-dashboard`，把真实 CLI 数据导出为前端可读 JSON。
