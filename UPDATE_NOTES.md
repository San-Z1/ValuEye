# 🚀 迭代更新笔记 (Update Notes)
- **✨ 核心功能升级**：新增近 7 日历史趋势展示、sparkline 走势摘要、综合估值面板和更清晰的本月定投计划输出。
- **🛠️ 架构与代码重构**：把项目整理成明确的包结构，拆出 `history.py` 和 `insights.py`，主流程只负责编排；同时保留旧入口兼容层，避免迁移断档。
- **🐛 细节与健壮性优化**：补了 BaoStock / akshare 获取失败时的兜底处理，历史文件损坏时自动备份，测试也改成兼容包路径和直跑路径。
- **💡 架构师建议**：下一步可以把历史数据做成可选的本地缓存图表页，或者加一个 `--summary` 模式，方便只看结论不看明细。

